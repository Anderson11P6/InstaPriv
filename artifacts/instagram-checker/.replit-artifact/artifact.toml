kind = "web"
previewPath = "/"
title = "Instagram Account Checker"
version = "1.0.0"
id = "artifacts/instagram-checker"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 22948

[services.development]
run = "pnpm --filter @workspace/instagram-checker run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/instagram-checker", "run", "build" ]
publicDir = "artifacts/instagram-checker/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "22948"
BASE_PATH = "/"
